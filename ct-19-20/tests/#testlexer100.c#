=>return||'c'else%><= ident +*-{ ident ,"string"]/%>if123while(&&=> ident {||else123-}[!=+*else-]'c'==)==,</%sizeof,123}void+*%-,}structreturnif.<||."string"sizeof}<.char]#includewhile>=<+int<char]returnwhile%while'c'structreturnstructelse[{ ident (int[>=%charstructelseelse123>=.+||{sizeof<={return"string";/%&&===<=.>=+*{"string"123>%{%struct}+{char||]%!=<=sizeofwhile;returnreturn&&{123{/%elsevoidvoid{);+*<=(+*>/%=&&while+*[return#include}%];'c'&&(>=]=>=#include||'c'"string"-charif}}!=/%!=+while)-"string"==[<=+.(int&&)})+>=<=%struct%<sizeof>else]struct<while#include"string"=%while]else.'c'-#include&&-if ident ."string".}!=/%else!=whilesizeof[/%)int.> ident  ident >='c'=.}>=[>= ident struct/%)>{||}return]/%,,+*]return;==+char[int+struct.&&/%<=-int;{{+*-int"string"-%.>=;>+*void=+*[/%return)while=else||>+*'c'struct=sizeof#includeifvoid||,returnstruct[<=+*sizeofcharvoid=!=void!=(/%==-}ifelse<)"string"else;;=&&#include"string"==||( ident ;<{&&!=123"string"'c'int||[==structreturn+(-=return;||}void+char->=)while==||==char- ident != ident if=ifvoidifchar123(>=)-else==!=struct<!=>while;sizeof+*void!=/%>=void&&"string"struct;)+&&"string" ident 'c'<"string"#include[123while123struct{#include%struct&&void123=,int==return,+*"string"123-'c'>>=-(==];if<=%;>===while=;(/%+*||#include123if(+*123else.;<=%> ident [>=)'c'#include%.char=,intstruct/%#include>==][sizeofvoidstructsizeofelsevoidchar]]&&<&&,,char ident +*."string"/%||"string";(while+*-+*(sizeof;{!=("string">=whileifchar>[voidwhile-)sizeof)#includereturnsizeofsizeof(+*struct123return<=int123/%char,return<=if)(+*returnelse% ident 'c')#includechar=% ident /%===([%char;[=={%;!="string"void+*)&&<=).char)(/%#include>{.||!=%sizeof/%voidsizeof+*>{return>=+if<&&+ifelse;"string"123(!=int==] ident ([>}==>+*)123(.sizeof<=<={else=%char{;);returnifchar123-==+(%returnstruct+||while}->=},int>;.+#include['c'while,struct} ident ) ident sizeof%ifwhile&&!=%((+{<=}whilewhile"string"struct"string"{elsesizeof.=if[elsereturn%{sizeofchar ident <-sizeof] ident char#include=int,/%/%char123/% ident <='c'%+*>"string"else"string"{&&<<>=+*-ifwhile ident char"string""string"'c'.return==[#include#includevoid(#include123==+whilechar||-'c'struct.#include||==[=->=+<=[char=/%.+-><=], ident structreturn<>if#includestruct(]||-{[==,'c'char%;===]{%#include(,+&&('c'/%[+*!=-!===return%+if]'c' ident ==+*)char};(==<='c'sizeof[void,}!=return,&&;void#include]if)>}ifelse ident ==>=)+*)-./%;>=int/%#include=='c'<=<[whilewhile{+*whilesizeofint!=}voidchar<>'c'%/%{ ident -%<=else#include(+],.==<=[struct=[<)returnsizeofsizeof/%==int)<=!=if}ifelse;else]if%#include/%'c''c'.-'c'.elsesizeof-while==struct'c'%-,&&{struct'c'-123]struct+*/%{'c'elsechar||'c'<=>=#include{/%&&{123(>while<+*}(!=&&=%>[==#include[=sizeofreturn+*!=#include ident return=<=>=;>='c'}==structint+!="string"||=>={char123=||%ifelse!=-&&)int'c']=>= ident !=else(sizeof/%'c'if,]charelse&&}<[struct)"string"'c'"string"==char,if;char=>=(.if,!=returnreturn"string"<void%>=}(if%123)if-]'c'sizeof;>===||123[sizeof||sizeofif"string"int(else&&]>}123else, ident ,||else!==)void} ident return ident  ident +"string"(==char=.{!=;<123struct!=>!==-|| ident =>=char&&=else||.structwhilevoid&&-%sizeofvoid}>#include)>=(;!=<==123char,>=<=>,(char<}%int+*!=-sizeof,sizeofintchar= ident <=}+*#include#include>=%struct[(+)([}( ident 123).if[while)"string"[&&<[[return ident <=||charwhile+<={,else+*&&else/%"string")sizeofwhile%/%/%'c' ident return!=||int)==struct}='c'void("string"||void'c'<!=while{.!=int]>=/%||123/%return(-==/%., ident &&&&123#includeelse{+*&&"string"&& ident "string"[#include<int"string"[%%}void%<=#include/%,if!=+*</%<.>while{;&& ident (struct>=void=-('c'!=whileelse%void<=sizeof]}<;+*#include'c'>=while'c'+*#include]charvoid123<char<=||sizeofif||||="string"-||->)%.==void}=struct%%%intstruct[!=sizeof(=sizeof+||% ident  ident (int||struct>=charwhilereturn ident (void/%int](&&.==charelse{return/%123sizeof!=[,return==(%]=%int]=>=123<<'c'>void+.,#include]{!=,,=int+*<=>='c'"string";>sizeof[>==>=!=+123>=struct#include;char;&&[,>=-- ident ||>={<='c'%ifchar<=123while<)"string"123+)voidint%int.]<=.%else'c'==char.<{ifstruct>int[;struct.<=<=;,(<=&&intint ident ,123char<ifvoid+*if>===]char'c'void123]else-/%%"string"struct+*{#includewhile ident &&/%sizeof ident =char}123char.123struct[ifvoidvoid+[&&-123]"string"<whileelse.(else ident ( ident #includestruct"string"<=]returnelse+*<==[if123>;((/%[ ident else123char||[&&/%>=!=char"string"||<|| ident {||while%void{==struct>!=#include]char"string""string"==if{struct&&123|| ident ==) ident ==[#includeelseint]||]struct)>>=[;sizeofif(='c'+if.)sizeof#include ident intstructsizeof==[[<;sizeof>=!=%>=else%if]!=,.int&&void<=voidreturn#include];>;/%<=&&)<=if<=]>}{"string"while==}{-+*,'c'void{}#include}char;+*||elsecharreturn]-char[if[/%%>==-<<=char;-+#include{int+*123] ident .char({"string"&&return{-voidelsestruct>=[if+*)[!=[(<while+*(<}+*>,struct-)char!=&&(else=char>=charchar==void>=<=returnintsizeof123struct+*return"string",while&&<=-[+*123#include);struct>=>while[123>sizeof[;else({>&&<==while,<sizeofreturnstruct[&&-'c'return=-sizeof("string"#includewhile123]structsizeofwhile==sizeof ident whilewhile&&/%while.return,sizeof!===<+*||else123))<=else-] ident [{{#include<=[;123/%'c')structreturn+*char ident >elseifsizeof+/%while<=.=+* ident return+else123 ident /%<=+*/%char(]voidelse!=struct>=>while-'c'-else%return123]while(]{charcharelse/%<!='c'"string"char(#include+*structstructint||char'c'voidint<=123<'c'struct%)&&}struct<+;>=%-/%=whilevoid+>=(+*void=== ident voidelseif||!=/%charreturn})voidwhile,>>char>=>>{/%'c'[else<int#includesizeof&&.%==intstruct"string"<) ident /%{if<=sizeof}!=(}<= ident else,return!=&&,else+*123returnwhile(int,return,%-==<=int+*>=+-[while<=123if=||/% ident %return<=+;,<char{%==-sizeof+intvoid>}||<=}char/%%)(()[whilechar>{"string"if>]"string"int/%ifelse+int#include123{%!=voidwhilewhile+)['c'struct>=while||intstructreturnif=sizeof123||==<=+*.void&&,, ident =123return.ifvoid>==whilewhile>=>,123intsizeof,123else-.while ident +*#include'c'return!=[returnchar+*/%-(}123<=>%struct>=>('c''c''c'char+if;>.<123char+],]voidchar<sizeof>="string" ident ||>||<=123int>123void+else)int'c'>123|| ident ->char||>.+ ident 123,&&/% ident ]voidint<='c'{&& ident void/%void+*+[ ident ||<while<=+}!=[int'c'>=#include[if,||||&& ident if}-return=voidvoid#include+*structelse<--sizeof'c'struct} ident {{<=,char"string".}{-/%]"string"(,void#include{<=+*if123.void,>[=)int]123&&if{==if%if<'c'elsereturnelsewhile,'c'&&char<};char#includecharwhile}struct==,/%.whilevoid123while%((returnelse'c'[=int ident return(.sizeof ident ifelse ident ||>charstruct#include<&&voidvoidsizeof/%&&<ifcharvoid#include}+-charif-returnvoid ident <=-<}+{'c'123if;%'c';.=)123&& ident }]char;+,]else#include#include||-struct['c'})/%while."string")char=%sizeof&&struct"string""string"!=||=else[-,<=structelse.>=%sizeof;"string"123123return})}=."string"/%#include/%% ident while,%=( ident void||123[["string"void;char>-char=char{else]=['c',)(sizeof ident !=]>=#includestruct'c' ident .void<=/%if;char;||return>=,!=>=="string"while123!=structint)-[char+&&voidwhile=!="string"struct,"string""string"{intsizeofstruct||while"string">]return,]==while)!=)whilewhilestruct||if>="string"<=-void<void=]>/%&&%else=%,<=%if#include ident voidint-||void{return+*>+}.>=-!=123=||%!=====<=while#include+"string".==[- ident 'c'> ident (+"string"while<=+*<#include{123]<=while&&'c'whilewhilesizeofstruct!=if123]sizeof=charvoid+/%;;void<=<=void123elsestruct'c'!=||/%return%+char=!=],int/%sizeofif123, ident .returnvoid#include123#include%,),!=>else/%{charsizeof.void>=struct.}voidcharchar(char ident <=){sizeof=[)/%[{+*<+*123 ident >=char[int ident <=<whileint'c'|| ident >||#include&& ident  ident  ident /%'c'123int;!=else)123sizeofelsewhile.||+!=||%==<=]/%==struct] ident ;+*%{].int],elsechar.<+123"string"char<=char>'c'.while ident ||+else}!=!=||123[>&&[/%),<=-void%.+charvoid{)char}-'c'&&sizeof[<=[||else==={===/%/%()%if)>='c'(!=void/%+==sizeof}sizeof%==(123int%#include(void ident if)< ident =[<={.+=-int!=.=<return'c'||</%if[if>;'c'char>return||<=(#includeint[struct;ifreturn<else!=||"string"/%&&'c'%)return{!==while<charstruct123'c'==]( ident [while&&>+}{<=,#includeelse"string""string"int ident else>=/%}"string"{void[return<==else!=return"string">=123>charreturn{sizeof+==return;=.struct%;>#include ident >sizeof=='c''c'+*'c'+int;void>||-sizeof{.charif{charstruct.}+{>='c';}123)if]while<)int%+<(;,sizeof{=={=else],}if>=+*])!===,["string"+'c'(<=sizeof[.voidreturn#includeelse"string"return%{>'c'%elsereturn)=]"string">{+<>[]=struct&&'c'},!={>==}sizeofwhilevoidvoid;==]sizeof<=}<(if==elseint;<=struct+"string">=<=[ ident else"string""string"[if]==int||else123sizeof#includestructsizeof[void<void!=+||)!=/%(else-#includewhile%==if'c','c'[)/%#include!=if[.#includestruct.==while){-%==<if)>{char]struct==else%,]return'c'(,&& ident {struct||&&.while>=],{return==+*>+='c'(char"string"int+*>=else+void||=#include!=->=else]!=!=void=if!=(int,if)"string">={.+*,!=,}sizeof123intvoid]<=else==<+#includeintchar.>char"string""string"123if{<=+'c'=+while)+*>=;while!=%else[%[.'c'if.>=}.charelse||[(>=>]% ident 'c'void'c'while=/%char<={>if{ifreturnsizeof123<=+<struct!=+*123>=-'c';+*,#include-sizeofvoid})>if<=while%whileint<<;(elsecharifvoid,{;>=+&&&&123}-+*+*<===[}int[returnif"string"..int<=>struct) ident #include}#include.,}else}+*struct{#include=&&%"string")'c'=<while<void."string"sizeof{.+)"string"123),>=if!=%++return;<<='c'!=>!=||||<if{'c'+*- ident {<<{!=};||>==+*;<=#include123!=struct#include."string"while}{int,#include123!=struct(><=returnelse(< ident <=<(-(else++*<}.[&&sizeof,voidint>=while&&+*"string".%int ident if&&'c''c'{+,!=while"string"+*int/%=#includeint,<=ifsizeof,char)<] ident void}>'c'#includeint."string"<=)struct ident <{+return<=.[ ident char}[=#includesizeof%)<'c'<== ident )!= ident 123<=sizeof[if+==int([struct<=/%-;;!=while||<123{(<intreturn)struct.{(;<=&&'c'=struct.%%&&123-||====int<,else ident +*elsechar+*#include<voidint)123if,void"string"+* ident 'c'else#includeif#include||>=<{123while]void==voidreturn>"string"void123/%elsecharchar>][+*/%<&&struct=||#include=%/%int%return/%,)void+else<=<=&&int{123123->==={+char}>int"string";voidif[/%}+*,-"string"<return(]['c'return123!='c'+*#include.>=,{int"string"-charsizeofsizeof>= ident =structvoid123<else>==||structsizeof||if123&&>=("string"<=>= ident >if123+*[.{"string"while+="string"-, ident {'c'void}"string"=char&&&&)'c'%char||+#include{].if} ident returnstructwhilechar)while||;<=#include,&&;||== ident 123%)}+intstruct+while/%<=struct"string"char]&&charchar-][)/%"string"123{whilewhile123&&,{>{return||||]}else"string"/%+<=<=)+(#include}structreturn>=;'c'&&==charreturn"string"<-<]123||=#includestruct#include}"string"/%.123>='c'if&&&&sizeof!=>elseelse%> ident struct(if.%>struct ident while<==return"string"%})void#includechar-returnsizeof'c'.else"string";sizeof.sizeof;+ ident <)+*}"string"sizeof<='c'==/%int"string"char123,if>-[%{+*.>({'c'/%+(#includereturn123%{ifvoid||#include}!=while[&&&&char>((return>%||]-||-(elsechar!=,%'c'++>;;"string"+*{=<#include#include,&&charvoid{!=!=%"string"%>=>=voidif; ident ||||,[else%,+*>=char;'c'(,/%if.<||=return+]'c'sizeof123(sizeof'c'>whileelse[]-=='c' ident ;sizeof||.,struct+*intvoidreturnreturn(int,==struct{sizeof#include>else[#include/%(else>while#include,>=||] ident [/%);&&123%; ident %({'c'>=+,.ifreturn{[while123<>=>char,(!=;"string" ident +>===struct ident <[;+*123"string"},%(struct}-"string"(<+ ident } ident {+*!='c'!=]struct#include#include}]return)returnvoidwhilechar(sizeof&&.sizeof,&&]#includecharcharelse'c']}if.[%if'c'+-struct<returnelse;void123==intchar-+*else.#include#includeelsevoid,int}-struct'c'||else<] ident /%!=#include#include%<ifchar#include123return<=+%<"string"#include#include123else||struct'c'while)struct&& ident returnchar"string";=+(int{#include!=+*voidstruct+]-void==#include&&}(&&sizeof(%/%=<==='c'+int<>+*123'c'return&&int]/%((>%>+&&||#include-(<=}>=#includeintreturn||void{struct)<.sizeof#include<(><. ident ifsizeof!=if]while'c'returnsizeof123[sizeofsizeof-if+*(-{ ident &&else{#include== ident -#include ident [<intstruct{-else||int,sizeofelse'c'int!=(<if ident ]sizeof+*)+*})=sizeof<=struct(<%||void{<=-=-(=<="string" ident char&&!=&&!=(%#include[==]==structif;}<=,!=.int&&+==(!=->='c'ifvoid<="string"<=+*&&intwhile+*voidchar<=>=.#include123void==void+*void&&<=%)||&&ifstruct123;elsereturn]sizeof;>=sizeofintsizeof}[>=/%if>.(;>=[struct"string";>=} ident [..sizeof-/%if>=return<>=if(elseelse"string"charint-<+*struct=,!=)=||.void||)=int,+*structwhile;,===-char'c'||'c'returnstruct+*&&#include ident +*return+*-struct<+%<+)voidwhilevoid->=>=(++*==returnvoidsizeof)123{return'c'{>)[intwhile(struct{return.,)='c'(<&&{[%struct.})<=)intvoid)void#include ident !=&&elsestruct-],&&voidchar=[ ident <123;>=>=++*void==ifif}<sizeof>)#includewhile&&struct+('c'!=(intint;intchar"string"{(if#includereturn(return.void'c'{sizeof<{== ident &&[-&&"string""string"sizeof!=+<=[if}while}+*!=<=%==return[while[char'c')returnstruct)+123123void;[void+.'c'+*sizeof==>=charchar=!=if=;returnchar=;+*==+123>=={[,-else}/%==>]"string"char+*>===<-,>=void||returnsizeof,;#includewhile||!=void>=sizeof;>else}'c'/%else<====123ifsizeof<=<={))%structif==<=int&&sizeof>=}<){whileelse+*[("string",-"string" ident <=."string"/%+*.%>=)==&&<<)ifwhile<= ident %.sizeofstruct{=!=-<=)else||<=;==if==void"string"[+sizeof"string"!=)sizeof>=intreturn</%{({[!=<>=sizeof123'c'123'c'===;[sizeof>=>=>=+*+*),));/%)+.-whilesizeof==.>=ifwhilesizeof<=-ifsizeofint123{sizeofif&&void.voidwhilevoid123'c'123||'c'struct) ident (.!=== ident ]123intifcharint%(voidifchar[while(if#includestruct#include; ident [struct/%#include"string"||charifstruct/%'c'/%.if