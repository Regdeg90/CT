struct e{
    int x;

 
};

struct x {
    int x;
    struct e p;
};

struct e e; //Are we allowed this
char x;
int f[3];
int* t;


int hi(int x) {
// has no return of int type
}

void main() {
    char r;
    char* e;
    int x;
    int y;
    
    x = hi(x);
    x = hi(hi(hi(x)));
    e = sizeof(int).hello;

    x = main(); // x is now void but passes?
    if (x) {
        if (y) {

        } else while(x != e.x) {

        }
    } else if (f[1] != f[3]) {

    }

    return;
}


