// #include "hi"
#include "minic-stdlib.h"
// #include "studio.h"
struct s {
    int x;
    char y;

};
struct e{
    int x;
    char y;
    struct s e;

};
char* c;
struct e e;
int x;

// int xper(int *y){
//     char* c;
//     c = y; 
//     return 0;
// }
int xper(int *b){
    int *p;
    return xper(p);
}
int main(int y, char w) {
    
    int x;
    struct e p[4];
    // print_s(c);
    // x = print_s(c);
    while(x != x) {
        char x;
        struct e e;     
        // print_i(x).hiya= 3;
        // print_s(c);
        e.x = 5;
        // x = x + 2;
        p[1] = e;
        
        if (x == x) {
            int x;
            char* y;
            int z[6];
            int* p;
            int i;
            char c;

            i=0;
            *p=i;

            y = (char*)"hello";

            // *c = "hello";
            // x = (int)c;
            // p = (int*)z;
            // x = x - 3 + 5 *(4/3);
            // // print_s(y);
            // // x = read_c();

            // e.x = 5;
            // func();

            print_i((int) c);
          
        } 
        else {
            int x;
            // struct c s;
            // print_i(y);
            return x;
            
        }
    }
    // print_i(c);
    return x;
}